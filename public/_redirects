# Force HTTPS
http://* https://:splat 301!

# www -> root
https://www.bye-tatouage.com/* https://bye-tatouage.com/:splat 301!

# Remove trailing slash (except root)
/*/ /:splat 301!

# SPA fallback for client-side routing
/* /index.html 200
